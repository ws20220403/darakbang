# 다락방 v4 — 변경사항 & 검토 보고서

v3 패키지(폴더명을 v4로 변경)에 이어 요청하신 **4가지**를 반영했습니다.
디자인 언어(보라 브랜드 · Notion 스타일 · 다크모드)와 기존 동작은 유지했고,
이전(v1~v3) 변경 내역은 `다락방_v3_변경사항.md`를 참고하세요.

> **빌드 불필요** — 순수 HTML/CSS/JS. 실행: 폴더에서 `py -m http.server 8080` (이 PC는 `python` 대신 **`py`** 사용).

---

## 요약

| # | 요청 | 상태 |
|---|------|------|
| 1 | 표: **전체폭·기본 2×2** 로 생성, 행/열 추가는 **가장자리 ＋(v3 방식)**, 함수 힌트는 **= 입력 시 표시(사칙연산 포함)**, **천단위 콤마(토글)** | ✅ |
| 2 | 본문에서 **하위문서 링크 삭제 → 좌측 탭에서도 제거**(루트/하위 모두, 안전장치 포함) | ✅ |
| 3 | **‘다락방 사용법’** 문서를 현재 기능에 맞게 업데이트(v3 어투 유지) | ✅ |
| 4 | 최종 **성능·안정·동기화 속도 검토 및 검증** | ✅ |

모든 항목은 브라우저 자동화로 검증, **콘솔 오류 0건**입니다.

---

## 1. 표 — 양식은 기존(전체폭), 기능은 v4

- **전체폭 · 기본 2×2** — 새 표는 입력 영역 가로폭을 꽉 채워 2×2(머리글 1행)로 생깁니다(기존 ‘표’와 동일한 느낌).
- **행/열 추가(기존 방식)** — 표 **오른쪽 ＋(열)**, **아래 ＋(행)** 버튼으로 추가. (별도 +행/+열 버튼 툴바 제거)
- **함수 힌트는 ‘=’ 입력 시에만** — 셀이 `=` 로 시작하면 열(A·B·C)/행(1·2·3) 좌표와 함께 **사용 가능한 함수가 명확히** 뜹니다.
  - `함수 =SUM · AVERAGE · MAX · MIN · COUNT · PRODUCT · ROUND · ABS  |  사칙연산 + − × ÷ ( )  |  범위 A1:B3` (누락됐던 사칙연산 포함)
- **천단위 콤마** — 함수로 계산된 숫자를 `733,679` 처럼 끊어 표시. 표에 마우스를 올리면 나오는 작은 툴바의 **`1,000`** 버튼으로 **켜고 끌 수 있음**(기본 켜짐). 직접 입력한 숫자는 그대로 둡니다.
- 그 작은 툴바에 **머리글 / 천단위콤마 / 행 삭제 / 열 삭제** 를 모았습니다(평소 숨김, 호버 시 표시 → 표가 깔끔).
- 셀 클릭 시 수식, 벗어나면 계산값. Tab/Enter 로 셀 이동. 옛 ‘계산표’ 블록 데이터는 표로 자동 변환.

> 코드: `blocks.js`(`TableTool`), `formula.js`(공용 수식 엔진), `css/blocks.css`.
> [선택] 엑셀식 ‘= 입력 후 셀 드래그로 범위 선택’은 기존 편집과 충돌 위험이 있어 이번에도 보류(요청 단서 반영). 좌표 라벨을 보며 직접 입력합니다.

## 2. 본문 ↔ 좌측 탭 ‘삭제’ 동기화

- 본문에서 **하위 문서 링크 블록을 지우면**(영역 선택 후 Backspace) 해당 하위 페이지가 **좌측 탭에서도 사라집니다.**
  - **루트/하위 모두 동일** 적용(하위의 하위까지). 구글 모드는 **드라이브 휴지통**으로 안전 삭제(복구 가능), 데모는 로컬에서 제거.
- **오삭제 방지 안전장치**:
  - 로드 시점의 본문 링크를 스냅샷으로 기억해, **이번에 실제로 지운 링크만** 반영(원래 링크가 없던 예전 자식은 건드리지 않음).
  - 전환·파괴 레이스로 본문이 잠깐 비어 보일 때(블록 0개)는 무시 → 대량 오삭제 방지.
  - 일반 텍스트 편집으로는 하위 페이지가 지워지지 않음(자동 검증).
- **순서 변경 로직은 그대로** 유지(요청대로 변경하지 않음). 즉 “탭에서 위치 변경 → 본문 반영”, “본문 순서 변경 → 탭 반영”은 v3 동작 그대로.

> 코드: `app.js`(`_reconcileChildrenWithBody`, 본문 링크 스냅샷 `_bodyLinks`).

## 3. ‘다락방 사용법’ 문서 업데이트

- 현재 기능에 맞춰 **11개 섹션**으로 새로 정리(어투·구성은 v3 안내문을 따름).
  - 글쓰기/서식 → 블록 추가 → **표와 함수(좌표·콤마·가장자리 추가 설명 + 계산 예시 표)** → 순서 바꾸기 → **하위 페이지 자동 연동(생성/양방향 순서/삭제 동기화)** → 페이지 순서 변경 → 저장&단어수 → 페이지 관리/내보내기 → 검색/즐겨찾기 → 구글 연결 → 로그인/데모.
- 데모 시드 버전을 올려(데모도 갱신), 실모드 첫 실행에도 이 문서가 일반 노트로 생성됩니다.

## 4. 성능 · 안정 · 동기화 검토

- **빠름**: 35블록 문서에서 `editor.save()` 약 **8ms**, 페이지 전환 **3–34ms**.
- **누수 없음**: 여러 번 전환해도 에디터 인스턴스는 항상 1개(이전 에디터 정상 파괴).
- **즉시 동기화**: 본문↔좌측 탭 순서/삭제가 자동저장(1.5s)을 기다리지 않고 ~0.4초 내 반영.
- **안정화**: 페이지 전환/저장/단어수 경로 정리, 빠른 연속 전환 가드, 빈 데이터 가드. 미사용 `@editorjs/table` 의존성 제거(더 가벼움).
- 자동 검증에서 **콘솔 오류 0건**.

---

## 바뀐 파일 (v4 라운드)
```
js/blocks.js     TableTool 전면 개편(전체폭·2×2·가장자리 ＋·함수힌트·천단위 콤마·호버 툴바)
js/app.js        _reconcileChildrenWithBody(본문 링크 삭제→트리 제거) + 본문 링크 스냅샷
js/samples.js    ‘다락방 사용법’ 11섹션 최신화(+검색 키워드)
js/local-store.js  데모 시드 버전 ↑ (사용법/예시표 갱신)
css/blocks.css   표 전체폭·＋버튼·호버 툴바·함수힌트(= 시 표시) 스타일
index.html       버전 갱신
```

## 검증 요약 (데모 모드 · 브라우저 자동화)
- (1) 새 표 2×2·전체폭, 가장자리 ＋ 추가, = 입력 시 좌표+함수힌트, 콤마 `733,679`/토글, 콤마는 함수결과에만
- (2) 본문 링크 삭제 → 트리 제거(루트/하위/중첩), 일반 편집·빈데이터 시 오삭제 없음
- (3) 사용법 11섹션 + 예시 표 계산(합계/이익, 콤마) 정상
- (4) save 8ms·전환 3–34ms·에디터 누수 0·콘솔 오류 0
