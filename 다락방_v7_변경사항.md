# 다락방 v7 — 변경사항 & 검토 보고서

v6 패키지를 복사해 만든 **새 폴더 `다락방_v7_배포패키지`** 입니다(기존 v6 보존, 데이터 충돌 방지).
이전(v1~v6) 내역은 `다락방_v6_변경사항.md` 등을 참고하세요.

> **빌드 불필요** — 순수 HTML/CSS/JS. 실행: 폴더에서 `py -m http.server 8080` (이 PC는 `python` 대신 **`py`**).

---

## 요약

| # | 요청 | 상태 |
|---|------|------|
| 1 | **‘변환’ 종류 확장** — 텍스트·제목·목록·체크리스트·인용구(5종)에 **코드·토글** 추가, 변환 목록 화면도 맞춰 확장 | ✅ |
| 2 | **단축키·편의기능 안내** — 적절한 위치에 버튼 + 편의기능별로 그룹화한 안내 창 | ✅ |
| 3 | **‘다락방 사용법’ 새로 작성** — 현재 모든 기능을 담은 상세 설명서 | ✅ |
| 4 | **최종 보안·안정성 점검** — 불필요한 복잡/꼬임 정리 | ✅ (콘솔 오류 0) |

데모 모드 브라우저 자동화로 전 기능 검증, 신규 콘솔 오류 0건.

---

## 1. ‘변환’에 코드·토글 추가 + 변환 목록 확장

**원리 파악:** 이 앱의 ‘변환’은 **Editor.js 기본 변환기**입니다. 변환기는 블록에 `conversionConfig`(상호 변환 규칙)가 정의된 블록만 목록에 보여줍니다. 기본 5종(텍스트·제목·목록·체크리스트·인용구)만 그 규칙을 갖고 있어 5개만 보였던 것입니다.

**한 일**
- **토글**(`js/blocks.js`의 `ToggleTool`)에 `conversionConfig` 추가 — 다른 블록 ↔ 토글 상호 변환.
  - 토글 → 텍스트 등: 제목과 내용을 합쳐 텍스트로.
  - 텍스트 등 → 토글: 글자를 토글 제목으로(펼친 상태로 생성).
- **코드**(CDN `@editorjs/code`)는 원본에 변환 규칙이 없어, 기능은 그대로 물려받고 **변환 규칙만 더한 래퍼**(`DarakCodeTool`, `js/blocks.js`)를 만들어 `js/editor.js`에서 등록.
  - 코드 → 텍스트 등: 특수문자(`< > &`)는 안전하게 escape, 줄바꿈은 보존.
  - 텍스트 등 → 코드: 서식 HTML을 순수 텍스트로 풀어 코드로(아래 4번의 보안 처리 참고).
- **변환 목록 화면 확장**(`css/editor.css`) — 항목이 5 → 7개로 늘어도 잘리거나 좁게 스크롤되지 않도록 변환(중첩) 팝오버를 넉넉히 펼침(`max-height: min(74vh, 460px)`, 최소 폭 220px). ‘추가(+/ /)’ 그리드 메뉴는 영향 없음.

**결과:** 글자를 선택하면 뜨는 도구막대(또는 블록 손잡이 ⠿ → 변환)의 **변환 목록에 코드·토글이 함께** 나오고, 7종이 한눈에 보입니다.

## 2. 단축키 · 편의기능 안내 창 (`js/shortcuts.js` 신규)

- 본문 상단 줄에 **⌨️ 단축키 버튼**을 추가(되돌리기/복원 버튼 옆). 누르면 안내 창이 열립니다.
- 안내 창은 **편의기능별로 그룹화**:
  1. **블록 추가·변환** (`/`, `+`, 글자 선택→변환, ⠿→변환 / 변환 가능 7종 안내)
  2. **자동 변환 — 기호** (`->`→→ 등 9종)
  3. **자동 변환 — 줄머리(마크다운)** (`#`/`-`/`1.`/`>`/```` ``` ````/`[]`)
  4. **편집 단축키** (`Ctrl/⌘+S`, `Ctrl/⌘+Z`, `Ctrl/⌘+Shift+Z`, 선택 서식, Enter, Tab, Esc …)
  5. **표 안에서** (방향키·Tab·Enter 셀 이동, `=` 함수, `'` 텍스트 고정 / 함수 목록)
  6. **블록·페이지 관리** (⠿ 드래그/클릭, 사이드바 ⋯ 메뉴)
- 키는 **kbd 칩**으로 보기 좋게 표시. 닫기는 **닫기 버튼 · Esc · 바깥 클릭** 모두 가능.
- 내용은 전부 **정적 데이터**라 사용자 입력이 섞이지 않고, 표시는 `textContent`로 넣어 안전.

## 3. ‘다락방 사용법’ 전면 개편 (`js/samples.js`)

- 버전 변경 이력이 아니라, **지금 다락방이 가진 모든 기능**을 처음 쓰는 사람도 따라 할 수 있게 담은 **설명서**로 다시 썼습니다(17개 절, 맨 위 **목차(TOC)** 자동 생성).
- 다룬 내용: 시작하기(데모/로그인) · 화면 구성 · 글쓰기와 서식 · **자동 변환**(기호/마크다운) · **블록 추가(전체 15종 표)** · **블록 변환(코드·토글 포함)** · **표와 함수** · 블록 순서 · 하위 페이지(양방향 연동) · 페이지 순서 · 저장/되돌리기·복원 · 페이지 관리·내보내기·백업 · 검색·즐겨찾기 · 테마·화면 · **단축키 한눈에** · 구글 드라이브 연결 · 로그인/데모/데이터 위치.
- 설명서 안에서 **토글·코드·표·콜아웃·체크리스트** 블록을 실제로 보여줘 바로 익히도록 구성.
- 데모 시드 버전 `8 → 9`(`js/local-store.js`)로 올려 데모에서 새 사용법이 반영되게 함. (실제 구글 모드는 기존 ‘사용법’ 페이지가 있으면 덮어쓰지 않음 — 사용자가 고친 내용 보존.)

## 4. 보안 · 안정성 · 복잡도 점검

- **변환 시 XSS 차단:** 텍스트 등 → 코드 변환에서 서식 HTML을 텍스트로 풀 때 `innerHTML` 대신 **`DOMParser`(비활성 문서)** 사용. 이미지 `onerror` 등 스크립트가 **실행되지 않음**을 자동화로 확인(`xssFired: false`). (입력 자체도 Editor.js가 이미 정화하지만, 한 겹 더 안전하게.)
- **CDN 실패 내성:** 코드 래퍼(`DarakCodeTool`)는 **원본 `CodeTool`이 있을 때만 정의**하고, `editor.js`는 없으면 원본으로 **폴백**. 코드 CDN이 빠져도 `blocks.js` 전체가 깨지지 않음.
- **여는 클릭이 곧장 닫는 문제 예방:** 안내 창의 ‘바깥 클릭 닫기’는 **여는 클릭이 끝난 다음 틱**에 연결(기존 이모지 피커와 동일한 방식) → 버튼을 눌러 연 창이 같은 클릭으로 닫히지 않음.
- **외부 의존 0 추가:** 새 CDN·라이브러리 없음. 추가 파일은 순수 JS `js/shortcuts.js` 1개뿐.
- **변경 범위 최소화:** 기존 기능/구조는 그대로 두고 필요한 곳만 손봄. 캐시 갱신을 위해 `index.html`의 `?v=` 토큰을 일괄 갱신.
- **검증(데모·브라우저 자동화):** 모듈 로드·렌더 정상, **콘솔 오류 0건**. 변환 목록 7종 노출, 텍스트→토글·문단→코드 실제 변환, 사용법 60블록(전 유형) 렌더, 안내 창 6그룹 열고/닫기, 새 페이지 코드·토글 삽입·저장까지 모두 정상.

---

## 바뀐 / 추가된 파일 (v7)
```
js/shortcuts.js   [신규] 단축키·편의기능 안내 창(그룹화, kbd 칩)
js/blocks.js      ToggleTool.conversionConfig 추가, DarakCodeTool(코드 변환 래퍼·DOMParser·CDN 가드) 추가
js/editor.js      code 도구를 DarakCodeTool 로 등록(미정의 시 CodeTool 폴백)
js/samples.js     ‘다락방 사용법’ 전면 개편(현재 전 기능 상세 설명서, 17절+목차)
js/local-store.js 데모 시드 버전 8 → 9(새 사용법 반영)
js/app.js         단축키 버튼(#btn-shortcuts) 클릭 → Shortcuts.open 연결
css/editor.css    ‘변환’ 목록(중첩 팝오버)·변환 툴바 화면 확장
css/components.css 단축키 안내 모달 스타일(.modal-lg, .sc-*)
index.html        단축키 버튼·안내 모달 마크업, shortcuts.js 로드, ?v= 캐시 갱신
```

## 실행
```bash
# 이 폴더에서
py -m http.server 8080
```
브라우저에서 `http://localhost:8080` → **“데모로 둘러보기”**.
