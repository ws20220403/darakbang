# 다락방 v5 — 변경사항 & 검토 보고서

v4 패키지를 복사해 만든 **새 폴더 `다락방_v5_배포패키지`** 입니다(데이터 충돌 방지).
이전(v1~v4) 내역은 `다락방_v4_변경사항.md` 등을 참고하세요.

> **빌드 불필요** — 순수 HTML/CSS/JS. 실행: 폴더에서 `py -m http.server 8080` (이 PC는 `python` 대신 **`py`**).

---

## 요약

| # | 요청 | 상태 |
|---|------|------|
| 1 | 개선분을 **v5 새 폴더**로 | ✅ |
| 2 | 표 **‘1,000’ 을 표 전체에 적용** + 적용 시 **소수점 자동 반올림** | ✅ |
| 3 | 표에서 **방향키로 셀 이동**(엑셀식) | ✅ |
| 4 | 셀 맨 앞 **`'` → 텍스트 고유값**(`'=D5+D6` → `=D5+D6` 그대로) | ✅ |
| 5 | **Enter = 입력완료 + 아래 셀로**(표 위에 빈 줄 생기던 문제 수정) | ✅ |
| 6 | **문서 이동 속도(특히 하위문서) 개선** 원인 파악·해결 | ✅ (Drive 경로 최적화) |
| 7 | 모든 기능 반영한 **‘다락방 사용법’** 갱신 | ✅ |
| 8 | 안정화·속도 추가 조치 | ✅ |

데모 모드 자동화로 검증, 신규 콘솔 오류 0건.

---

## 2. 표 ‘1,000’ (천단위 콤마) — 표 전체 + 반올림

- **표 전체에 적용**: 켜면 함수 결과뿐 아니라 **직접 입력한 숫자까지** 표 안의 모든 숫자를 천단위 콤마로 표시.
- **소수점 자동 생략(반올림)**: 켜져 있으면 `66.666…` → `67`, `1234.56` → `1,235` 처럼 정수로 반올림해 표시.
- **토글**: 표에 마우스를 올리면 나오는 작은 툴바의 **`1,000`** 버튼으로 켜고 끔.
- **기본값 OFF**: 이제 입력값까지 반올림하므로, 켜기 전엔 입력 그대로 보입니다(원할 때만 켬). 셀을 클릭하면 항상 원래 값/수식이 보입니다.

## 3. 표 셀 방향키 이동 (엑셀식)

- **↑/↓**: 위/아래 셀로 이동. **←/→**: 셀 안에서 커서를 움직이다 **글자 끝에 닿으면** 옆 셀로 이동.
- **Tab**(오른쪽, Shift+Tab 왼쪽), **Enter**(아래)도 그대로.

## 4. `'` 로 시작 → 텍스트 고유값 (엑셀식)

- 셀 맨 앞에 **`'`** 를 붙이면 나머지를 **계산하지 않고 그대로 글자**로 둡니다.
  - 예: `'=D5+D6` → 화면엔 `=D5+D6` 로 표시(수식 계산 안 함). 숫자도 `'0123` 처럼 텍스트로 둘 수 있음.
- 셀을 클릭하면 편집을 위해 `'` 까지 포함한 원문이 보입니다.

## 5. Enter 동작 수정

- 셀에서 **Enter → 입력 완료 + 바로 아래 셀로 이동**(마지막 행이면 편집 종료).
- 기존에 **표 위에 빈 줄이 생기며 표가 내려가던 문제**를 수정(에디터가 Enter 를 새 블록 생성으로 가로채지 않도록 차단).

## 6. 문서 이동 속도 개선 (특히 하위문서)

**원인:** 데모(브라우저 저장)는 이동이 2~14ms 로 빠릅니다. 느림은 **구글 드라이브 모드의 네트워크 왕복**에서 발생합니다. 특히 처음 여는 페이지마다 드라이브 **파일 검색(`findFile`) 쿼리**가 1건씩 붙어 느렸습니다.

**해결(기존 동작 보존):**
- **(A) 시작 시 페이지 파일 ID 일괄 캐시** — 앱을 열 때 `pages` 폴더 목록을 **한 번**에 받아 모든 페이지의 파일 ID를 캐시합니다. 이후 어떤 페이지를 처음 열어도 **개별 검색 없이 바로 읽기** → 첫 이동이 크게 빨라집니다. (실패 시 기존 개별검색으로 자동 대체)
- **(B) 자동저장 시 충돌검사 읽기 생략** — 이동/자동저장 같은 ‘조용한 저장’은 저장 전 원격을 다시 읽던 단계를 건너뜁니다(네트워크 1왕복 절감). **수동 저장(Ctrl+S/저장 버튼)은 충돌검사를 그대로 유지**해 안전.

> 두 조치 모두 **데모 모드엔 영향 없음**(드라이브 전용 경로). 기능 손상 없이 네트워크 왕복만 줄였습니다. 실제 체감 개선 정도는 구글 모드에서 확인해 주세요.

## 7. ‘다락방 사용법’ 갱신

표 섹션에 **방향키 이동 · `'` 텍스트 · Enter · 1,000(표 전체+반올림)** 설명을 추가했습니다(전 11섹션, 어투는 기존 유지). 데모 시드 버전을 올려 데모/실모드 모두 갱신.

## 8. 추가 안정화

- **무한 멈춤 방지**: 에디터가 준비되기 전(또는 비정상 상태)에 저장/이동이 호출돼도 **최대 8초만 대기 후 안전 종료**하고, **빈 데이터로 덮어쓰지 않도록** 했습니다(데이터 손실·멈춤 방지).
- 빠른 연속 전환에도 **에디터 인스턴스 1개 유지**(누수 0), 전환 2~14ms.

---

## 바뀐 파일 (v5)
```
js/drive.js       시작 시 페이지 파일 ID 일괄 사전캐시(_preloadPageFileIds)
js/workspace.js   savePage 에 충돌검사 생략 옵션
js/app.js         자동저장 충돌검사 생략, 미준비 시 빈저장 방지
js/editor.js      getEditorData: isReady 8초 타임아웃 + null 반환(무한멈춤·빈저장 방지)
js/formula.js     ' 접두 → 텍스트(isFormula/displayValue)
js/blocks.js      표: 콤마 표전체+반올림, 방향키 이동, Enter 수정, ' 텍스트
js/samples.js     ‘다락방 사용법’ 표 섹션 갱신
js/local-store.js 데모 시드 버전 ↑
index.html        버전 갱신
```

## 검증 (데모 모드 · 브라우저 자동화)
- (2) 콤마 OFF: `733679`/`1467358` 그대로, ON: `733,679`/`1,467,358`(반올림+콤마, 표 전체)
- (3) ↑↓←→ 셀 이동 정상
- (4) `'=A1+B1` → `=A1+B1` 텍스트(계산 안 함), 포커스 시 원문 표시
- (5) Enter → 아래 셀, 블록 수 불변(빈 줄 안 생김)
- (6) 데모 이동 2~14ms, Drive 경로는 findFile 제거·충돌읽기 생략으로 왕복 감소
- (7) 사용법 11섹션 + 표 예시 계산
- (8) 에디터 누수 0, 무한멈춤 방지
```
