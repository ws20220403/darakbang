# 다락방 v8 — 변경사항 & 검토 보고서

v7 패키지를 복사해 만든 **새 폴더 `다락방_v8_배포패키지`** 입니다(기존 v7 보존, 데이터 충돌 방지).
이전(v1~v7) 내역은 `다락방_v7_변경사항.md` 등을 참고하세요.

> **빌드 불필요** — 순수 HTML/CSS/JS. 실행: 폴더에서 `py -m http.server 8080` (이 PC는 `python` 대신 **`py`**).

---

## 요약

| # | 요청 | 상태 |
|---|------|------|
| 1 | `>` + 칸 단축키를 **인용구 → 토글**로 변경, 단축키 안내·사용법 반영, 나머지 단축키 점검 | ✅ |
| 2 | 빠른 입력/삭제 시 **연동 지연** 원인 분석 + 개선 | ✅ 분석·개선 |
| 3 | 토글 **제목 Enter → 아래 새 토글**(목록식), 토글 **내용에 글머리(목록)** 적용 | ✅ |
| 4 | 토글 **내용 Enter → 토글 안에서 줄바꿈**(밖으로 안 나감) | ✅ |
| 5 | 기존 기능 충돌 검증 · 코드/파일 효율화 · v8 마무리 | ✅ (콘솔 오류 0) |
| 6 | **‘다락방 사용법’**에 새 기능까지 모두 반영 | ✅ |

데모 모드 브라우저 자동화로 전 기능 검증, 신규 콘솔 오류 0건.

---

## 1. `>` 단축키: 인용구 → 토글 (`js/autoformat.js`)

- 빈 줄 맨 앞에서 `>` + 칸을 입력하면 이제 **토글**이 만들어집니다(기존 인용구에서 변경). 인용구는 추가(+/ /) 메뉴·변환으로 계속 사용할 수 있습니다.
- **단축키 안내 창**(`js/shortcuts.js`)의 줄머리 마크다운 표에 `>` → **토글**로 갱신, 토글 Enter 동작 2줄 추가.
- **나머지 단축키 점검** — 기호 자동변환 9종(`->,<-,=>,<=,!=,...,(c),(r),(tm)`), 줄머리(`#/##/###`, `-`/`*`, `1.`, ```` ``` ````, `[]`), 편집/표/관리 단축키가 실제 코드와 일치함을 확인(불일치 없음).

## 2. ‘연동 지연’ 원인 분석 + 개선

**원인 분석(측정):** 입력이 멈추면 `editorChanged`(디바운스)로 목차·사이드바·되돌리기 스냅샷 등 ‘연동’이 갱신됩니다. 68블록 문서에서 한 번의 연동 비용을 재보니 저장 직렬화 ≈3.8ms, 스냅샷 ≈0.3ms 로 **연산 자체는 가볍고**, 체감 지연의 실제 원인은 ① 갱신까지의 **대기(400ms)**, ② **목차(TOC) 블록이 내용이 바뀔 때마다 DOM 전체를 다시 그려** 깜빡임/리플로우를 유발, ③ 자동저장마다 **사이드바 전체를 불필요하게 재렌더**하는 부분이었습니다.

**개선**
- **반영 지연 단축** — `editorChanged` 디바운스 **400 → 250ms**(`js/editor.js`). 연속 입력 중엔 발화하지 않아 부하 없음.
- **목차 비파괴 갱신** — 제목 목록이 그대로면 다시 그리지 않도록 서명(sig) 비교(`js/blocks.js` TOC). 본문만 칠 때 목차가 **깜빡이지 않음**. 또 첫 로드 때 형제 헤더가 늦게 준비되는 경우를 위해 **로드 직후 한 번 더 채움**(있으면 무시) → 목차가 처음부터 보입니다.
- **자동저장 군더더기 제거** — 조용한 자동저장에선 **사이드바/브레드크럼 전체 재렌더 생략**(`js/app.js`). 제목은 입력 즉시 동기화되고 구조 변경은 각 경로에서 이미 렌더하므로 안전.

## 3. 토글 — 제목 Enter(아래 새 토글) + 내용 글머리 (`js/blocks.js`)

- **제목에서 Enter** → 바로 **아래에 새 토글**을 만들고 그 제목으로 포커스 이동(‘목록’식). **빈 제목**에서 Enter면 그 자리에서 **일반 문단으로 빠져나갑니다**(목록의 빈 항목 종료와 동일). 기존엔 위쪽에 빈 줄이 생기던 동작을 바로잡았습니다.
- **내용 안 글머리(목록)** — 줄 맨 앞에서 `- `/`* ` → **•**, `1. ` → 번호로, **줄머리 자동변환 방식** 그대로 적용. Enter로 다음 글머리가 이어지고, 빈 글머리에서 Enter면 해제됩니다. (별도 라이브러리 없이 토글 내용 영역에서 처리)

## 4. 토글 — 내용 Enter는 토글 안에서 줄바꿈 (`js/blocks.js`)

- 토글 **내용에서 Enter** 를 누르면 토글 밖으로 나가지 않고 **그 안에서 줄바꿈**됩니다(`stopPropagation` 으로 Editor.js 의 ‘새 블록’ 동작을 막고 줄바꿈만 삽입). 한글 조합 중·`Shift+Enter` 는 기본 동작 유지.

## 5. 충돌 검증 · 효율화 · 안정화

- **회귀 검증(데모 자동화):** `>`→토글 · 토글 제목/내용 Enter · 토글 글머리 · 기호 자동변환(`->`→→) · 변환(코드·토글 conversionConfig) · 페이지 이동 · 목차 자동갱신 모두 정상, **콘솔 오류 0건**.
- **🐞 되돌리기 멈춤 버그 수정(중요)** — Editor.js 의 `blocks.render` 프로미스가 **토글/콜아웃이 있으면 resolve 되지 않는** 이슈가 있어, 토글이 든 문서에서 `Ctrl+Z`(되돌리기)가 **영영 멈출 수 있었습니다**. `>`→토글로 토글이 흔해진 만큼, `history.js`의 적용부를 **타임아웃 경쟁**으로 보호해 되돌리기/복원이 멈추지 않게 했습니다(DOM 은 즉시 그려지므로 안전). 검증: 토글 포함 문서 되돌리기 → 정상 복귀·잠금 없음.
- **효율화** — 목차 비파괴 갱신·자동저장 재렌더 생략·디바운스 정리로 입력 중 불필요한 DOM 작업을 줄였습니다. 새 외부 의존 0, 추가 파일 0(기존 파일만 수정).

## 6. ‘다락방 사용법’ 갱신 (`js/samples.js`)

- 좌측 **‘다락방 사용법’** 페이지(데모 첫 화면)에 새 동작을 모두 반영: 줄머리 마크다운의 `>` → **토글**, 블록 표의 토글 설명(제목 Enter/내용 Enter/내용 글머리), **토글 예시 블록**을 펼친 상태로 바꾸고 내용에 사용법을 직접 담음.
- 데모 시드 버전 **9 → 10**(`js/local-store.js`)으로 올려 데모에서 새 사용법이 반영되게 함. (실제 구글 모드는 기존 ‘사용법’ 페이지가 있으면 덮어쓰지 않아 사용자가 고친 내용은 보존)

---

## 바뀐 파일 (v8)
```
js/autoformat.js  '>' 줄머리 → 토글(기존 인용구)
js/blocks.js      ToggleTool: 제목 Enter=아래 새 토글/빈 제목=문단, 내용 Enter=안에서 줄바꿈,
                  내용 글머리('- '→•, '1. ' 이어가기); TOC 비파괴 갱신 + 로드 직후 보강
js/history.js     되돌리기/복원 적용 시 blocks.render 타임아웃 경쟁(토글/콜아웃 멈춤 방지)
js/editor.js      editorChanged 디바운스 400 → 250ms
js/app.js         조용한 자동저장에서 사이드바/브레드크럼 전체 재렌더 생략
js/shortcuts.js   '>' → 토글로 안내 갱신, 토글 Enter 동작 안내 추가
js/samples.js     사용법에 토글 새 동작·'>'→토글 반영(토글 예시 펼침)
js/local-store.js 데모 시드 버전 9 → 10
index.html        ?v= 캐시 토큰 갱신(v8)
```

## 실행
```bash
# 이 폴더에서
py -m http.server 8080
```
브라우저에서 `http://localhost:8080` → **“데모로 둘러보기”**.
