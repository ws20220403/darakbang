# 다락방 v9 변경사항 (2026-06-23)

> v8 배포패키지를 복사해 만든 새 폴더입니다. 요청 4건을 반영했습니다.
> 캐시 버전(`?v=`)은 `20260616-v8e` → **`20260623-v9a`** 로 올렸습니다.

---

## 요청 요약

1. 페이지/하위 페이지 **생성 속도** 단축 (목표 3초 이내)
2. 페이지 안에서 **저장 → 연동 시간** 단축 (목표 2초 이내)
3. **백스페이스** 동작 정리 (토글·목록·체크리스트 등 추가기능 줄)
4. 창을 오래 켜두면 나는 **"토큰 갱신 실패 / 팝업 열기 실패"** 오류 진단·보완

---

## 1) 페이지 생성 속도 — 불필요한 네트워크 왕복 제거 + 병렬화

### 원인
- 새 페이지를 만들 때 `drive.js`의 `writePage → writeJsonFile`가 **항상 `findFile`(존재 검색)을 먼저** 호출했습니다. 새 페이지는 당연히 없으므로 **순수 낭비 1왕복**.
- `createPage`가 `writePage`(검색+생성) → `_saveWorkspace`(검색+수정)를 **순차로** 기다렸고, 하위 페이지는 이동 시 부모를 한 번 더 저장해 **총 5왕복** 정도가 직렬로 발생.
- 생성 직후 제목 포커스에 **`setTimeout 300ms`** 고정 지연.

### 변경
- **`js/drive.js`**
  - `writePage(pageId, data, opts)` 재작성:
    - 파일 ID를 이미 알면(`_pageFileIds`) `findFile` 없이 **바로 PATCH** (저장 전반 가속 — 2번에도 효과)
    - `opts.create` 면 검색 없이 **바로 생성**(새 UUID라 중복 위험 없음)
  - `writeWorkspace`/`writeSettings` 도 캐시된 파일 ID가 있으면 **`findFile` 생략하고 바로 PATCH**.
- **`js/workspace.js`**
  - `createPage` / `duplicatePage`: 본문 저장 + 워크스페이스 저장을 **`Promise.all` 병렬**, 새 페이지엔 `{ create: true }` 전달.
  - `_createStarterPage`: `{ create: true }` 전달.
- **`js/app.js`**
  - `createNewPage`의 제목 포커스: `setTimeout(…, 300)` → **`requestAnimationFrame`**(즉시 다음 프레임).

### 효과(예상)
- 루트 페이지 생성: 약 3왕복 → **약 1왕복**.
- 하위 페이지 생성: 약 5왕복 → **약 2왕복**.
- 한국–구글 드라이브 왕복(150~300ms) 기준 3초 이내로 충분히 들어옵니다.

---

## 2) 저장 연동 시간 — 디바운스 단축 + 저장 자체 가속

### 원인
- 자동저장 디바운스가 **1500ms**. 저장 1회가 `findFile`×2 + 쓰기×2 = **4왕복 직렬**.

### 변경
- **`js/app.js`**: `AUTOSAVE_DELAY` **1500 → 900ms**.
- **`js/workspace.js`**: `savePage` 의 본문/워크스페이스 저장을 **`Promise.all` 병렬**.
- **`js/drive.js`**: (1번과 동일) 캐시 ID 직접 PATCH 로 저장마다 `findFile` 2회 제거 → 저장이 **4왕복 → 약 1왕복**.

### 효과(예상)
- 입력 멈춤 후 연동: 약 250ms(에디터 디바운스) + 900ms + 약 1왕복 ≈ **1초 초반**. 2초 이내 충족.

---

## 3) 백스페이스 — 빈/특수 블록에서 "문단으로 풀기"

### 정의(확정)
- 대상 블록: **토글·목록·체크리스트·콜아웃·인용구**.
- **블록의 첫 줄 맨 앞**에서 Backspace 를 누르면 — 윗줄로 탈출해 윗줄이 지워지던 기존 동작을 막고 —
  그 줄을 **일반 문단으로 변환**합니다. **윗줄은 절대 건드리지 않습니다.**
  - 빈 줄 → 빈 문단(=추가기능 해제). 한 번 더 누르면 그때 표준 병합(윗줄로).
  - 글자 있는 줄 → **글자를 유지한** 문단으로(서식만 제거, 글자 손실 없음).
  - 목록/체크리스트가 여러 줄이면 **첫 줄만 문단으로 빼고 나머지는 목록 유지**(분할, 체크 상태·중첩 보존).
- 줄 중간이나 둘째 줄 이후의 Backspace 는 **가로채지 않습니다**(평소대로 글자 삭제/항목 병합).

### 변경
- **`js/editor.js`**: `#editorjs` 에 **캡처 단계 keydown** 리스너(`_setupBackspaceGuard`) 추가.
  - 캐럿이 **블록 첫 편집영역의 맨 앞**(앞에 글자 없음)일 때만 개입 → `preventDefault`+`stopPropagation` 으로
    Editor.js/도구 기본 백스페이스 차단 후 `_unwrapToParagraph()` 로 문단 변환.
  - 블록 데이터는 `block.save()` 로 읽어 토글(제목+내용)·목록·체크리스트 구조를 안전하게 처리.
  - `init`/`destroy` 시 리스너 정리(누수 방지).

### 검증(데모, 합성 + 실제 콘텐츠)
- 빈 토글 → 빈 문단 / 글자 토글("안녕") → 문단 "안녕"
- 단일 목록("사과") → 문단 / 다중 목록(하나·둘·셋) → 문단 "하나" + 목록(둘·셋)
- 단일/다중 체크리스트 → 문단 + 체크리스트(체크 상태 보존)
- 콜아웃("중요!") → 문단
- 실제 콘텐츠 목록(첫 항목 `<b>표에 함수</b>…`) → **볼드 보존**하며 문단 + 나머지 3항목 목록 유지
- **윗줄(헤더/문단) 모든 경우 무손상**, 줄 중간 백스페이스는 가로채지 않음, 콘솔 에러 0.

---

## 4) "토큰 갱신 실패 / 팝업 열기 실패" — 원인과 보완

### 원인(확정)
- 구글 액세스 토큰은 **약 1시간** 후 만료됩니다. 창을 오래 켜두면 **자동저장/저장**이
  `auth.js`의 `_silentRefresh → requestAccessToken({prompt:'none'})` 를 호출하는데,
  구글이 조용한 갱신을 못 하면 **팝업으로 폴백**합니다. 이 호출은 사용자 클릭이 아니라
  **타이머(자동저장)** 에서 비롯되므로 브라우저가 팝업을 차단 → **`popup_failed_to_open`**.
- 게다가 기존 코드는 실패 시 `clearTokens()` 로 **세션까지 지워** 더 불편했습니다.

### 변경
- **`js/auth.js`**
  - **선제 갱신**: 토큰 발급 시 만료 **2분 전** 조용히 갱신 예약(`_scheduleProactiveRefresh`).
    탭이 보일 때만 시도하고, **탭 복귀(visibilitychange)** 시 만료 임박이면 조용히 갱신 →
    대부분의 경우 토큰이 만료 상태로 방치되지 않아 오류 자체가 잘 안 납니다.
  - **오류 분류**: 팝업 차단/상호작용 필요(`popup_failed_to_open` 등)면 `code:'reauth_required'` 부여.
    이 경우 **세션을 지우지 않고**(작성 내용·로그인 보존) 재연결 안내로 보냅니다.
  - **`reconnect()`** 추가: **사용자 클릭(제스처)** 안에서 `requestAccessToken({prompt:''})` 호출 →
    팝업이 정상적으로 열려 토큰 갱신. `needsReauth()` 로 상태 조회.
- **`js/app.js`**
  - 저장 실패가 재인증 오류면 오류 토스트 대신 **"다시 연결" 버튼 배너**(`_promptReconnect`)를 띄우고,
    클릭하면 `Auth.reconnect()` 후 **실패했던 저장을 자동 재시도**.
  - **재연결 대기 중에는 자동저장 보류**(오류 토스트 반복/스팸 방지). 작성 내용은 그대로 유지.
- **`js/ui.js`**
  - 버튼이 달린 비소멸 토스트 `actionToast()` 추가(테마색 버튼). 재연결 안내에 사용.

### 효과
- 평상시: 선제 갱신으로 오류가 거의 발생하지 않음.
- 갱신이 막혀도: **데이터 손실/세션 삭제 없이** 버튼 한 번으로 복구 + 저장 이어가기.

> 참고: 실제 OAuth 흐름은 배포 도메인(`ws20220403.github.io`)에서만 동작하므로 로컬 데모로는
> 끝까지 재현이 어렵습니다. 코드 경로·배너 UI·자동저장 보류는 데모에서 확인했습니다.

---

## 변경 파일
- `js/drive.js` — 저장/생성 시 `findFile` 생략(캐시 ID PATCH, 새 페이지 직접 생성)
- `js/workspace.js` — 저장/생성 병렬화, 새 페이지 `create` 힌트
- `js/app.js` — 자동저장 900ms, 생성 포커스 즉시화, 재연결 처리/자동저장 보류
- `js/auth.js` — 선제 갱신, 오류 분류, `reconnect()`/`needsReauth()`, 세션 보존
- `js/ui.js` — `actionToast()` (버튼 토스트)
- `js/editor.js` — 백스페이스 가드(특수/빈 블록 → 문단으로 풀기)
- `index.html` — `?v=` 를 `20260623-v9a` 로 갱신

## 배포 방법
1. 이 폴더(`다락방_v9_배포패키지`)의 내용을 GitHub 저장소(`darakbang`)에 덮어쓰기.
2. 커밋·푸시 → GitHub Pages 반영.
3. `?v=`를 이미 올렸으므로 브라우저 캐시는 자동으로 새로 받습니다.

## 검증
- 로컬 `py -m http.server`(포트 8105) + 데모 모드 자동화로 확인.
- 페이지/하위 페이지 생성·저장·검색 인덱스·백스페이스 전 케이스 정상, **콘솔 에러 0**.
