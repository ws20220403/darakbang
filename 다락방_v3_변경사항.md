# 다락방 v3 — 변경사항 & 검토 보고서

v2 배포 패키지를 기반으로, 요청하신 5가지 항목을 구현/검토했습니다.
디자인 언어(보라 브랜드 · Notion 스타일 · 다크모드)와 기존 동작은 그대로 유지했고,
새 폴더 `다락방_v3_배포패키지`에 담았습니다. (v2 폴더는 그대로 보존)

> **빌드 불필요** — 순수 HTML/CSS/JS. `python -m http.server` 로 바로 실행됩니다.

---

## 요약 (한눈에)

| # | 요청 | 상태 |
|---|------|------|
| 1 | 좌측 탭에서 하위 페이지 추가 시 **부모 본문 맨 아래에 링크 생성** (중첩 포함) | ✅ 구현·검증 |
| 2 | 본문에서 하위 페이지 **순서 변경 → 좌측 탭 순서도 동일하게** | ✅ 구현·검증 |
| 3 | **페이지(루트) 간 순서 변경** — 드래그&드롭 + 설정의 위로/아래로 (하위페이지 제외) | ✅ 구현·검증 |
| 4 | 파일(PDF/Word/hwp/Excel) 업로드 + 표 함수(SUM 등) **가능성 검토 후 진행** | ✅ 둘 다 구현(검토 결과 포함) |
| 5 | 그 외 안정적으로 유용한 기능 검토 | ✅ 4종 구현 + 추가 제안 |

모든 항목은 데모 모드에서 자동화 검증을 통과했고, 콘솔 오류 0건입니다.

---

## 1. 하위 페이지 추가 시 본문에 링크 자동 생성

**문제(v2):** 좌측 탭의 `⋯ → 하위 페이지 추가` 로 만들면 사이드바에는 보이지만,
부모 페이지 **본문 안에는 그 하위 페이지로 가는 링크가 없었음.**

**해결(v3):** 좌측 탭에서 하위 페이지를 만들면 **부모 본문 맨 아래에 '하위 문서' 링크 블록**이 자동으로 추가됩니다.

- 부모가 **현재 열려 있으면** → 라이브 에디터 맨 끝에 즉시 삽입(페이지 이동 시 자동 저장).
- 부모가 **닫혀 있으면** → 저장된 본문(JSON)에 직접 추가.
- **중첩도 동일** — 하위 페이지 안에서 또 하위 페이지를 만들어도 그 페이지 본문 맨 아래에 링크가 생깁니다. (최대 5단계)
- 하위 페이지를 **삭제하면** 부모 본문의 죽은 링크도 자동으로 정리됩니다.

> 관련 코드: `app.js`(`_addChildLinkToParent`), `workspace.js`(`appendChildLink`/`removeChildLink`)

## 2. 본문 순서 ↔ 좌측 탭 순서 동기화

본문에서 '하위 문서' 링크 블록을 **드래그로 재배치**한 뒤 저장하면(자동 저장 포함),
좌측 탭의 하위 페이지 순서도 **본문에 기록된 순서**를 그대로 따라갑니다.

- 저장 시 본문의 링크 순서를 부모의 `children` 순서에 반영(`_syncChildrenOrderFromBody`).
- 사이드바는 그 `children` 순서대로 렌더(`getChildren`).
- 본문에 링크가 없는 하위 페이지(예: 링크를 지운 경우)는 맨 뒤로 유지되며, 페이지 자체는 삭제되지 않습니다.

## 3. 페이지(루트) 간 순서 변경

요청대로 **두 가지 방법**을 모두 제공합니다. **하위 페이지에는 적용되지 않고 최상위 페이지끼리만** 동작합니다.

- **드래그 & 드롭** — 좌측 페이지 목록에서 **루트 페이지 행을 잡고** 위/아래로 끌면 순서가 바뀝니다(파란 드롭 표시선). 하위 페이지 행은 드래그되지 않습니다.
- **설정(⋯) 메뉴의 위로/아래로** — 루트 페이지의 `⋯` 메뉴에 **`위로 이동` / `아래로 이동`** 이 나타납니다. (맨 위/맨 아래에선 자동 비활성)

> `rootPageOrder` 에만 반영되므로 하위 트리는 그대로 따라 움직입니다.
> 관련 코드: `workspace.js`(`moveRootPage`/`reorderRootPage`), `sidebar.js`(`_makeRootDraggable`)

---

## 4. 파일 업로드 & 표 함수 — 검토 결과 + 구현

### 4-A. 파일 업로드 (PDF · Word · hwp · Excel · PPT · zip 등) — **가능, 구현 완료**

**검토:** 기존 이미지 업로드가 이미 임의 바이트를 Google Drive 에 올리는 구조라, **확장만으로 충분히 안정적**으로 구현 가능했습니다.

**구현:** 추가 메뉴(`+` / `/`)에 **`파일`** 블록을 새로 넣었습니다.
- 클릭 또는 드래그&드롭으로 업로드 → 파일 카드(아이콘·이름·용량·다운로드 버튼)로 표시.
- 확장자별 아이콘: PDF 📕 / Word·hwp 📝 / Excel·csv 📊 / PPT 📙 / 압축 🗜️ …
- 저장 위치: 구글 모드는 드라이브 `DARAKBANG/images` 폴더에 **원본 파일명 보존**, 데모 모드는 브라우저에 저장.
- 용량 한도: **구글 모드 50MB / 데모 모드 2MB**(localStorage 한계 때문). 초과 시 안내.

> 관련 코드: `blocks.js`(`AttachmentTool`), `drive.js`/`local-store.js`(`uploadFile`/`getFileBlobUrl`)

### 4-B. 표에 Excel 함수(SUM/AVERAGE/MAX/MIN 등) — **가능, '계산표' 블록으로 구현**

**검토:** 기존 `표` 블록(@editorjs/table)은 수식 엔진이 없고 셀이 단순 HTML이라, 거기에 함수를 직접 끼워 넣으면 **불안정**합니다.
그래서 **기존 표는 그대로 두고**, 함수가 필요할 때 쓰는 **별도의 `계산표` 블록**을 새로 만들었습니다. (기존 문서·기존 표에 영향 0)

**구현:** 추가 메뉴에 **`계산표`** 블록.
- 셀에 `=` 로 시작하는 수식 입력 → 자동 계산. 열은 A·B·C…, 행은 1·2·3… (엑셀과 동일한 좌표)
- 지원 함수: **SUM, AVERAGE(AVG), MAX, MIN, COUNT, PRODUCT, ROUND, ABS**
- 범위 `A1:B3`, 인자 나열 `A1,B2,5`, 사칙연산 `=A1+B2*2`, 괄호·음수까지 지원.
- 다른 계산 셀을 참조해도 **연쇄 재계산**, **순환참조는 `#ERR`** 로 안전 처리(무한루프 없음).
- 행/열 추가·삭제 버튼 제공. 셀을 클릭하면 원래 수식이 보이고, 벗어나면 계산값이 보입니다.

예) 매출/비용 표에서 `=SUM(B2:C2)`, `=AVERAGE(B2:B4)`, `=B2-B3` 가 즉시 계산됩니다.

> 관련 코드: `blocks.js`(`SpreadsheetTool` — 자체 토크나이저 + 재귀하강 파서, 외부 `eval` 미사용)

**남은 선택지(원하시면):** 기존 `표` 블록 자체에 '합계 행 자동 추가' 같은 보조 기능을 더하는 방향도 가능합니다. 다만 현재처럼 별도 블록이 더 안정적이라 우선 이렇게 구현했습니다.

---

## 5. 추가로 넣은 유용한 기능 (안정성 우선 선별)

아래 4가지는 위험이 낮고 바로 쓸모 있어 **함께 구현**했습니다.

1. **페이지 복제** — `⋯ → 복제`. 본문·아이콘까지 복사(하위문서 링크는 제외해 죽은 링크 방지). 하위 페이지를 복제하면 부모 본문에도 링크가 함께 생깁니다.
2. **Markdown 내보내기** — `⋯ → Markdown 내보내기`. 페이지를 `.md` 파일로 저장(제목·서식·표·체크리스트·계산표 등 변환).
3. **전체 백업(JSON)** — 사이드바 하단 ⤓ 버튼. 모든 페이지+설정을 한 개의 `.json` 으로 내려받기(데이터 안전장치).
4. **단어/글자 수** — 상단 우측에 현재 문서의 단어·글자 수 표시.

### 더 제안 (다음에 원하시면)
- **JSON 백업 가져오기(복원)** — 위 백업 파일로 되돌리기. (덮어쓰기 주의 UI 필요)
- **빠른 이동(Ctrl/⌘+P)** — 제목을 입력해 페이지로 바로 점프.
- **페이지 템플릿** — 회의록/할 일/주간계획 등 기본 틀.
- **상위 페이지 변경(다른 페이지로 이동)** — 트리 구조 재배치.
- **최근 편집 목록** — 사이드바에 최근 연 문서.

> 참고: 전체 백업(JSON)은 텍스트/구조를 담습니다. 이미지·첨부 **원본 파일 자체**는 구글 드라이브의 `DARAKBANG` 폴더에 그대로 보관됩니다.

---

## 검증 (로컬 데모 모드 · 자동화 확인)
- 부팅/3개 샘플 페이지, 콘솔 오류 0건
- (1) 부모 열림/닫힘/중첩 모두에서 본문 링크 자동 생성, 삭제 시 죽은 링크 정리
- (2) 본문 링크 순서 변경 → 사이드바 순서 일치
- (3) 위로/아래로 + 드래그 재배치(루트만, 하위 제외) 확인
- (4) 파일 업로드 왕복 + 확장자 아이콘, 계산표 SUM/AVERAGE/MAX/MIN/COUNT/PRODUCT·사칙연산·연쇄·순환참조(#ERR) 확인
- (5) 복제/Markdown/백업/단어수 동작 확인

## 바뀐/추가된 파일
```
index.html            컨텍스트 메뉴(위로/아래로·복제·내보내기), 단어수, 백업버튼, export.js, 버전갱신
js/export.js   [신규] 블록 → Markdown / 평문 변환
js/blocks.js          AttachmentTool(파일) · SpreadsheetTool(계산표) 추가
js/editor.js          위 두 블록 도구 등록
js/workspace.js       getChildren 순서, appendChildLink/removeChildLink, 루트 순서이동, 복제, 본문↔탭 동기화
js/app.js             부모 본문 링크 연결, 복제/내보내기/백업/단어수
js/sidebar.js         루트 드래그&드롭, 컨텍스트 메뉴 확장, 삭제 시 링크정리
js/ui.js              컨텍스트 메뉴 항목(위로/아래로/복제/내보내기) 처리
js/storage.js/drive.js/local-store.js   uploadFile/getFileBlobUrl 추가
css/blocks.css·sidebar.css·components.css  첨부/계산표/드롭표시선/단어수 스타일
```
