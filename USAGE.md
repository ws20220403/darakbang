# 🏠 다락방 (DARAKBANG) 사용 설명서

> 구글 계정 1개로 사용하는 나만의 비공개 노트 공간

---

## 📋 목차

1. [처음 시작하기 (1회 설정)](#1-처음-시작하기-1회-설정)
2. [로컬에서 테스트하기](#2-로컬에서-테스트하기)
3. [GitHub Pages에 배포하기](#3-github-pages에-배포하기)
4. [로그인 및 시작 화면](#4-로그인-및-시작-화면)
5. [페이지 기본 사용법](#5-페이지-기본-사용법)
6. [블록 편집기 사용법](#6-블록-편집기-사용법)
7. [사이드바 기능](#7-사이드바-기능)
8. [단축키](#8-단축키)
9. [자주 묻는 질문 (FAQ)](#9-자주-묻는-질문-faq)

---

## 1. 처음 시작하기 (1회 설정)

### Step 1 — Google Cloud Console 프로젝트 생성

1. [https://console.cloud.google.com/](https://console.cloud.google.com/) 접속
2. 상단 프로젝트 선택 → **새 프로젝트** 클릭
3. 프로젝트 이름: `darakbang` (자유롭게 설정) → **만들기**

### Step 2 — Google Drive API 활성화

1. 왼쪽 메뉴 → **API 및 서비스 → 라이브러리**
2. 검색창에 `Google Drive API` 입력
3. **Google Drive API** 선택 → **사용** 버튼 클릭

### Step 3 — OAuth 동의 화면 설정

1. 왼쪽 메뉴 → **API 및 서비스 → OAuth 동의 화면**
2. 사용자 유형: **외부** 선택 → **만들기**
3. 필수 입력:
   - 앱 이름: `다락방`
   - 사용자 지원 이메일: 본인 이메일
   - 개발자 연락처 이메일: 본인 이메일
4. **저장 후 계속** (범위, 선택 사항 모두 건너뜀)
5. **테스트 사용자** 탭 → **사용자 추가** → 본인 Google 이메일 추가
6. **저장 후 계속**

> 게시 상태를 **테스트**로 유지하세요. 1인용 비공개 앱이므로 Google 심사가 필요 없습니다.

### Step 4 — OAuth 클라이언트 ID 발급

1. 왼쪽 메뉴 → **API 및 서비스 → 사용자 인증 정보**
2. **사용자 인증 정보 만들기 → OAuth 클라이언트 ID** 클릭
3. 앱 유형: **웹 애플리케이션** 선택
4. 이름: `다락방 웹 클라이언트`
5. **승인된 JavaScript 출처** 에 추가:
   - `http://localhost:8080` (로컬 테스트용)
   - `https://{your-username}.github.io` (GitHub Pages 배포 시 추가)
6. **만들기** 클릭
7. 나타나는 **클라이언트 ID** 복사 (예: `123456789-abcdef.apps.googleusercontent.com`)

### Step 5 — 코드에 Client ID 삽입

`js/auth.js` 파일을 열어 아래 줄을 수정합니다:

```javascript
// 수정 전
const CLIENT_ID = 'YOUR_GOOGLE_CLIENT_ID_HERE.apps.googleusercontent.com';

// 수정 후 (복사한 Client ID로 교체)
const CLIENT_ID = '123456789-abcdef.apps.googleusercontent.com';
```

---

## 2. 로컬에서 테스트하기

> 반드시 웹 서버를 통해 열어야 합니다.  
> `file://` 프로토콜로 직접 열면 Google OAuth가 작동하지 않습니다.

### 방법 A — Python (추천, 별도 설치 불필요)

```bash
cd D:\Users\vmfort\.gemini\antigravity\scratch\darakbang
python -m http.server 8080
```

브라우저에서 `http://localhost:8080` 접속

### 방법 B — Node.js

```bash
cd D:\Users\vmfort\.gemini\antigravity\scratch\darakbang
npx serve . -p 8080
```

### 방법 C — VS Code Live Server 확장

VS Code에서 `index.html` 열기 → 오른쪽 하단 **Go Live** 클릭

---

## 3. GitHub Pages에 배포하기

```bash
# 1. Git 초기화
cd D:\Users\vmfort\.gemini\antigravity\scratch\darakbang
git init
git add .
git commit -m "Initial commit: 다락방 앱"

# 2. GitHub에 저장소 생성 후 push
git remote add origin https://github.com/{username}/darakbang.git
git branch -M main
git push -u origin main
```

**GitHub 저장소 → Settings → Pages**:
- Source: `Deploy from a branch`
- Branch: `main` / `/ (root)`
- **Save** 클릭

배포 URL: `https://{username}.github.io/darakbang/`

배포 후 **Google Cloud Console** 에서:
- **사용자 인증 정보 → 클라이언트 ID 편집**
- 승인된 JavaScript 출처에 `https://{username}.github.io` 추가

---

## 4. 로그인 및 시작 화면

### 로그인

1. 앱 접속 시 로그인 화면이 나타납니다
2. **구글 계정으로 로그인** 버튼 클릭
3. Google 팝업 창에서 본인 계정 선택
4. 권한 요청 허용 (Google Drive 파일 액세스)
5. 앱이 자동으로 시작됩니다

첫 로그인 시 Google Drive에 `DARAKBANG/` 폴더가 자동 생성됩니다.

### 로그인 유지

- 로그인 상태는 브라우저의 `localStorage`에 저장됩니다
- 브라우저를 닫았다가 열어도 약 **55분간** 자동 로그인됩니다
- 55분 후에는 자동 갱신을 시도하며, 실패 시 재로그인을 요청합니다

### 로그아웃

- 왼쪽 사이드바 상단 우측 **→ (로그아웃)** 버튼 클릭

---

## 5. 페이지 기본 사용법

### 새 페이지 만들기

- 사이드바 하단 **+ 새 페이지** 버튼 클릭
- 또는 환영 화면의 **첫 페이지 만들기** 버튼 클릭

### 페이지 제목 설정

1. 페이지 상단 큰 텍스트 영역 클릭
2. 제목 입력 (Enter 키로 에디터 본문으로 이동)

### 페이지 아이콘 변경

1. 제목 위의 이모지 아이콘 클릭
2. 이모지 피커에서 원하는 이모지 선택

### 커버 이미지 설정

1. 페이지 아이콘 옆 **커버 추가** 버튼 클릭
2. 이미지 파일 선택 (최대 10MB, JPG/PNG/GIF/WebP)
3. 커버 이미지 위에 마우스를 올리면 **커버 변경** / **커버 제거** 버튼 표시

### 페이지 저장

- **Ctrl+S** (Windows) / **Cmd+S** (Mac)
- 툴바 우측 **저장** 버튼 클릭
- 모바일: 상단 우측 저장 아이콘 클릭

미저장 변경사항이 있으면 제목 옆에 주황색 점(●)이 표시됩니다.

### 하위 페이지 만들기

1. 사이드바의 페이지에 마우스 올리기
2. 오른쪽 **⋮** 버튼 클릭 → **하위 페이지 추가** 선택

최대 5단계까지 중첩 가능합니다.

### 페이지 이름 바꾸기

사이드바 페이지 우클릭 → **이름 바꾸기** 클릭 → 새 이름 입력

### 페이지 삭제

사이드바 페이지 우클릭 → **삭제** 클릭 → 확인 모달에서 **삭제**

삭제된 페이지는 복구할 수 없습니다. 하위 페이지도 함께 삭제됩니다.

### 즐겨찾기

사이드바 페이지 우클릭 → **즐겨찾기에 추가**  
즐겨찾기 페이지는 사이드바 상단에 별도 섹션으로 표시됩니다.

---

## 6. 블록 편집기 사용법

### 슬래시(/) 명령어로 블록 추가

에디터 빈 줄에서 `/` 입력 → 블록 유형 선택

| 명령어 | 설명 |
|--------|------|
| `/텍스트` | 일반 문단 |
| `/제목 1` | 가장 큰 제목 (H1) |
| `/제목 2` | 중간 제목 (H2) |
| `/제목 3` | 작은 제목 (H3) |
| `/체크리스트` | 할 일 목록 |
| `/인용구` | 인용 블록 |
| `/구분선` | 수평 구분선 |
| `/토글` | 접기/펼치기 블록 |
| `/콜아웃` | 강조 박스 (7가지 색상) |
| `/이미지` | 이미지 삽입 |
| `/하위 문서` | 다른 페이지 링크 |

### 블록 편집 (인라인 툴바)

텍스트를 드래그하여 선택하면 인라인 툴바가 나타납니다:
- **B** — 굵게
- *I* — 기울임
- U — 밑줄
- `code` — 인라인 코드
- 🔗 — 링크 삽입

### 토글 블록

- 화살표(▶) 클릭으로 접기/펼치기
- 토글 제목 클릭으로 내용 편집

### 콜아웃 블록

- 왼쪽 이모지 클릭 → 아이콘 변경
- 오른쪽 색상 버튼 → 배경 색상 변경 (빨강/주황/노랑/초록/파랑/남색/보라)

### 이미지 블록

- 이미지 영역 클릭 → 파일 선택 창 열림
- 이미지를 직접 드래그하여 영역에 드롭
- 클립보드에 복사된 이미지를 **Ctrl+V** 로 붙여넣기
- 이미지 아래 캡션 영역에 설명 입력 가능
- 이미지는 Google Drive `DARAKBANG/images/` 폴더에 저장됨

### 하위 문서 블록

- 블록 추가 후 연결할 페이지 검색 및 선택
- 선택된 페이지는 클릭 가능한 링크로 표시됨

---

## 7. 사이드바 기능

### 페이지 트리 탐색

- 페이지 왼쪽 화살표(▶) 클릭 → 하위 페이지 펼치기/접기
- 페이지 클릭 → 해당 페이지로 이동

### 페이지 검색

- 사이드바 상단 검색창에 키워드 입력
- 모든 페이지의 제목에서 실시간 검색

### 테마 전환

- 사이드바 상단 우측 ☀️/🌙 버튼 클릭
- 라이트 모드 ↔ 다크 모드 전환

### 사이드바 너비 조절 (PC)

- 사이드바 오른쪽 끝 경계를 드래그하여 너비 조절 (180px ~ 400px)

### 모바일에서 사용

- 상단 좌측 ☰ 버튼으로 사이드바 열기/닫기
- 사이드바 외부 영역 터치로 닫기

---

## 8. 단축키

| 단축키 | 기능 |
|--------|------|
| `Ctrl+S` (Mac: `Cmd+S`) | 현재 페이지 저장 |
| `Ctrl+B` | 선택 텍스트 굵게 |
| `Ctrl+I` | 선택 텍스트 기울임 |
| `Ctrl+U` | 선택 텍스트 밑줄 |
| `/` | 블록 유형 선택 팝업 |
| `↑ ↓` | 슬래시 팝업에서 블록 선택 이동 |
| `Enter` | 슬래시 팝업 선택 항목 적용 |
| `Esc` | 슬래시 팝업 닫기 |

---

## 9. 자주 묻는 질문 (FAQ)

### Q. "Google Identity Services 로드 실패" 오류가 납니다

인터넷 연결을 확인하세요. VPN이나 방화벽 환경에서는 접속이 차단될 수 있습니다.

### Q. 로그인 팝업이 나타나지 않거나 즉시 닫힙니다

브라우저의 팝업 차단 설정을 확인하세요.  
`localhost:8080` 또는 배포 URL에서 팝업을 허용해주세요.

### Q. "Drive API Error 403" 오류가 납니다

두 가지를 확인하세요:
1. Google Cloud Console에서 **Google Drive API**가 활성화되어 있는지 확인
2. OAuth 동의 화면의 **테스트 사용자** 목록에 본인 이메일이 추가되어 있는지 확인

### Q. 데이터가 어디에 저장되나요?

본인의 Google Drive에 `DARAKBANG/` 폴더가 생성되고 아래와 같이 저장됩니다:
- `DARAKBANG/workspace.json` — 전체 페이지 목록
- `DARAKBANG/pages/{id}.json` — 각 페이지 내용
- `DARAKBANG/images/{id}.ext` — 업로드된 이미지
- `DARAKBANG/settings.json` — 앱 설정

### Q. 저장이 안 됩니다

인터넷 연결 상태를 확인하세요. 오프라인 상태에서는 저장이 불가능합니다.  
연결 복구 후 `Ctrl+S`로 저장하세요.

### Q. 세션이 만료됐다는 메시지가 납니다

구글 액세스 토큰은 약 1시간 후 만료됩니다. 로그인 버튼을 다시 클릭하세요.

### Q. 다른 기기에서 같은 데이터를 볼 수 있나요?

네. 같은 Google 계정으로 로그인하면 어느 기기에서든 동일한 데이터에 접근할 수 있습니다.  
단, 동시에 여러 기기에서 편집하면 나중에 저장된 내용이 덮어씌워집니다.

### Q. 페이지를 실수로 삭제했습니다

삭제된 페이지 파일은 구글 드라이브 **휴지통**에 일정 기간 보관됩니다.  
Google Drive → 휴지통에서 파일을 복원할 수 있으나, workspace.json의 메타데이터는 이미 삭제된 상태입니다.

### Q. 이미지가 로드되지 않습니다

이미지는 Google Drive에서 인증 토큰을 통해 불러옵니다.  
세션이 만료된 경우 재로그인 후 페이지를 다시 열면 이미지가 표시됩니다.

---

## 📁 파일 구조 참고

```
darakbang/
├── index.html          ← 앱 진입점
├── css/
│   ├── reset.css       ← CSS 초기화
│   ├── variables.css   ← 디자인 토큰
│   ├── layout.css      ← 전체 레이아웃
│   ├── sidebar.css     ← 사이드바
│   ├── editor.css      ← 에디터 영역
│   ├── blocks.css      ← 커스텀 블록
│   └── components.css  ← 공통 컴포넌트
├── js/
│   ├── ui.js           ← UI 유틸리티
│   ├── auth.js         ← Client ID 설정 필요
│   ├── drive.js        ← Google Drive API
│   ├── workspace.js    ← 페이지 상태 관리
│   ├── editor.js       ← 에디터
│   ├── sidebar.js      ← 사이드바
│   └── app.js          ← 앱 진입점
├── assets/
│   └── favicon.svg
└── README.md
```

*다락방 v1.0 · Google Drive 기반 1인용 노트 앱*
