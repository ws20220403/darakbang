# 다락방 v6 — 변경사항 & 검토 보고서

v5 패키지를 복사해 만든 **새 폴더 `다락방_v6_배포패키지`** 입니다.
이전(v1~v5) 내역은 각 `다락방_vN_변경사항.md` 를 참고하세요.

> **빌드 불필요** — 순수 HTML/CSS/JS. 실행: 폴더에서 `py -m http.server 8080` (이 PC는 `python` 대신 **`py`**).

---

## 🔧 후속 수정 (v6i, 2026-06-10)

‘전환’ 첫 버전(v6h)에서 보고된 3가지를 바로잡았습니다. (참고 영상: Notion에서 여러 블록을 드래그 선택 → ‘전환’ 하위메뉴로 일괄 변환하는 흐름을 레퍼런스로 분석)

| # | 요청 | 처리 |
|---|------|------|
| A | 설정(⠿)의 **‘변환’**, 텍스트 선택 시 **‘Convert to’**가 ‘전환’과 별도로 남아 있음 → **‘전환’으로 통합** | ✅ 네이티브 변환 UI 2종 제거 |
| B | **일괄 전환이 안 됨** — 범위를 잡아도 한 줄만 바뀜 | ✅ 다중 블록 선택 인식 수정 |
| C | ‘변환’의 **검색·UI 형태를 차용** | ✅ 검색창 + 아이콘 목록(네이티브 팝오버 형태) |

### A. 네이티브 변환 메뉴 제거 (‘전환’ 일원화)
- 인라인 툴바(텍스트 선택)의 변환 토글 `.ce-inline-toolbar__dropdown` 과, 블록 설정 팝오버의 변환 항목 `.ce-popover-item[data-item-name="convert-to"]` 를 **CSS로 숨김**(`css/editor.css`). 굵게·기울임 등 서식 버튼은 그대로 둡니다.
- 이제 양식 변경 입구는 **‘전환’ 버튼 하나**로 통일됩니다.

### B. 일괄 전환 버그 수정 (핵심)
- **원인**: 본문을 드래그해 여러 블록을 선택해도, ‘전환’ 버튼을 누르는 순간 Editor.js가 블록 선택 표시(`.ce-block--selected`)를 지워버려, 전환 대상이 **현재 블록 1개**로 좁혀졌습니다.
- **수정**: 버튼을 누르는 시점(mousedown)에 **네이티브 텍스트 선택 범위(`window.getSelection`)로 시작·끝 블록을 계산**해 그 사이 전부를 대상으로 잡습니다(버튼 mousedown 에서 `preventDefault` 하므로 선택이 유지됨). `js/editor.js`의 `_selectionBlockRange`/`_convertTargetIndices`.
- 검증: 문단 3개를 가로질러 선택 → 대상 `[1,2,3]` 인식 → 체크리스트로 전환 시 **항목 3개짜리 체크리스트 1개**로 병합(표·이미지 등 전환 불가 블록은 그대로 유지).

### C. ‘변환’의 검색·UI 차용
- ‘전환’ 메뉴를 네이티브 변환 팝오버 형태로 다시 만들었습니다 — **상단 검색창 + 아이콘·이름 세로 목록**. 한글/영문 키워드로 거르고(예: `제목`, `todo`), 방향키 ↑/↓ + Enter 로 선택, 없는 검색어는 ‘결과 없음’. 스타일은 앱의 기존 팝오버(추가 메뉴)와 동일 토큰을 사용해 이질감이 없습니다.

> 검증(데모 자동화): 네이티브 변환 2종 `display:none` 확인 · 다중 블록 선택 인식 · 일괄 전환 병합 · 검색 필터 동작 · 콘솔 오류 0건.

---

## 요약

| # | 요청 | 상태 |
|---|------|------|
| 1 | **되돌리기(Ctrl+Z)·다시(Ctrl+Shift+Z)** | ✅ 구현 |
| 2 | 우측 상단 글자수 → **← / → 버튼**(되돌리기/다시), HWP식 '묶음' 단위 | ✅ |
| 3·4 | **‘전환’ 버튼** 신설(추가·설정 옆) — 여러 블록 일괄 양식 전환, 분할/병합 | ✅ |
| 5 | 입력 중 **`->` → `→`** 등 자동변환 | ✅ |
| 6 | 최종 안정/속도 검토 | ✅ |

데모 모드 자동화로 검증, 신규 콘솔 오류 0건.

---

## 1·2. 되돌리기 / 다시 (← / → 버튼)

- 우측 상단 **글자수 표기를 ← / → 버튼으로 교체**했습니다. 버튼 또는 단축키로 동작:
  - **되돌리기**: `Ctrl/⌘ + Z` 또는 **←**
  - **다시(복원)**: `Ctrl/⌘ + Shift + Z`(또는 `Ctrl+Y`) 또는 **→**
- **HWP식 묶음 단위** — 글자 단위가 아니라, **입력이 잠시(약 0.6초) 멈출 때마다 한 덩어리**로 스냅샷을 만들어, Ctrl+Z 한 번에 **방금 친 어절 묶음**이 한꺼번에 되돌려집니다(효율적).
- **전환·블록 추가/삭제도 한 단계**로 되돌려집니다. 버튼은 되돌릴 게 없으면 자동 비활성. 페이지를 옮기면 히스토리는 새로 시작합니다.

> 구현: 스냅샷 기반 자체 히스토리(`editor.js`). 외부 플러그인 미사용.
> **안정화 핵심**: Editor.js의 `blocks.render` 프로미스가 일부 블록(토글 등)에서 끝나지 않는 이슈가 있어, 되돌리기/전환이 멈추지 않도록 타임아웃으로 보호했습니다.

## 3·4. ‘전환’ 버튼 (여러 블록 일괄 양식 변경)

기존 ‘변환(Convert to)’은 한 번에 하나씩·대상이 제한적이었습니다. 이를 개선해 **추가(+)·설정(⠿) 옆에 ‘전환’ 버튼**을 신설했습니다.

- **메뉴**: 텍스트·제목1·2·3·글머리목록·번호목록·체크리스트·인용구·**코드·토글·콜아웃**까지. 추가 메뉴처럼 **스크롤 없이 콘텐츠 수에 맞춰** 한 화면에 보입니다.
- **여러 블록 일괄**: 본문을 드래그해 여러 블록을 선택한 뒤 전환하면 한꺼번에 바뀝니다.
  - **전환 가능한 것만 바뀌고**(텍스트·제목·목록·체크리스트·인용구·코드·토글·콜아웃), **표·이미지·파일 등은 그대로** 유지됩니다. (예: [텍스트·목록·표·토글] → 체크리스트 선택 시 표만 유지)
- **분할/병합 규칙**:
  - **목록/토글 → 텍스트·체크리스트**: 묶여 있던 줄들이 **각각의 항목으로 분할**.
  - **여러 텍스트 → 목록/체크리스트/코드**: **하나로 병합**(목록·체크리스트는 항목들, 코드는 줄로). **→ 토글/제목/인용구**는 **개별**로 분류.
- 전환 결과는 **한 번에 되돌리기**(Ctrl+Z) 가능.

> 구현: `editor.js`의 `_convert`(블록→항목 추출 → 대상유형으로 재구성). 버튼은 에디터 툴바에 주입(툴바 생성 감시로 항상 표시).

## 5. 입력 중 자동 변환

코드 블록·표 셀을 제외한 본문에서 다음을 자동 변환합니다:

| 입력 | 결과 |
|---|---|
| `->` / `-->` | → |
| `<-` / `<--` | ← |
| `=>` | ⇒ |
| `...` | … |
| `(c)` `(r)` `(tm)` | © ® ™ |

## 6. 안정화 · 속도 검토

- **무한 멈춤 제거**: 위 `blocks.render` 타임아웃 보호로, 토글 등이 있어도 되돌리기/전환이 멈추지 않습니다.
- **누수 없음**: 반복 전환에도 에디터 인스턴스 1개, 페이지 전환 2~12ms.
- **가벼움**: 되돌리기 스냅샷은 페이지당 최대 100개로 제한(페이지 이동 시 리셋). 자동변환·툴바 감시는 매우 가벼운 검사(주입 후 무시). v5의 Drive 이동 최적화(파일ID 사전캐시·자동저장 충돌검사 생략)도 그대로 유지.
- 데모 자동화에서 **콘솔 오류 0건**.

---

## 바뀐 파일 (v6)
```
index.html        상단 글자수 → ←/→ 되돌리기·다시 버튼, 버전 갱신
js/editor.js      되돌리기/다시 히스토리, 자동변환, ‘전환’ 버튼/메뉴·로직, blocks.render 타임아웃 보호
js/app.js         ←/→ 버튼·단축키 연결, 단어수 제거
css/components.css ←/→ 버튼 스타일(단어수 스타일 제거)
css/editor.css    ‘전환’ 버튼·메뉴(자동높이 그리드) 스타일
js/samples.js     사용법: 되돌리기·전환·자동변환 안내 추가(12섹션)
js/local-store.js 데모 시드 버전 ↑
```

## 검증 (데모 모드 · 브라우저 자동화)
- (1·2) 입력/삽입 후 ← 되돌리기, → 다시; 전환도 한 단계로 되돌림; 묶음 단위; 버튼 활성/비활성; 페이지 이동 시 히스토리 리셋
- (3·4) [목록·표·토글] → 체크리스트: 표 유지 + 나머지 5항목 분할 / 텍스트들 → 목록 병합 / 텍스트들 → 토글 개별 / 메뉴 11종 자동높이
- (5) `->`→→, `<-`→←, `=>`→⇒, `...`→…, `(c)`→© (코드/표 제외)
- (6) blocks.render 멈춤 해결, 누수 0, 전환 2~12ms, 콘솔 오류 0
```
