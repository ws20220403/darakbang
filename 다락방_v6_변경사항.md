# 다락방 v6 — 변경사항 & 검토 보고서

v5 패키지를 복사해 만든 **새 폴더 `다락방_v6_배포패키지`** 입니다(데이터 충돌 방지).
이전(v1~v5) 내역은 `다락방_v5_변경사항.md` 등을 참고하세요.

> **빌드 불필요** — 순수 HTML/CSS/JS. 실행: 폴더에서 `py -m http.server 8080` (이 PC는 `python` 대신 **`py`**).

---

## 요약

| # | 요청 | 상태 |
|---|------|------|
| 1 | **되돌리기(Ctrl+Z) / 복원(Ctrl+Shift+Z)** 적용 가능성 검토 + 구현 | ✅ 적용 |
| 2 | 우측 상단 **글자수 표기 삭제** → 그 자리에 **← / → 버튼**(되돌리기/복원), hwp식 “묶음 단위” 되돌리기 | ✅ |
| 3 | 입력 중 **자동 변환** — `->`→→ 외 `=>` `<=` 두꺼운 화살표, `[]`→체크박스, Notion식 마크다운 등 | ✅ |
| 4 | 최종 **무거움 · 불안정 · 동기화 지연** 점검 | ✅ (변경당 +0.04ms, 콘솔 오류 0) |

데모 모드 브라우저 자동화로 검증, 신규 콘솔 오류 0건.

---

## 1. 되돌리기 / 복원 — 검토 결과 “적용 가능” → 구현 완료

**검토:** 이 앱의 본문 편집기는 **Editor.js**(CDN)인데, Editor.js는 블록 추가/삭제/이동까지 아우르는 되돌리기를 기본 제공하지 않습니다(브라우저 기본 실행취소는 한 블록 안에서만 동작). 그래서 **문서 스냅샷 방식의 자체 되돌리기 매니저**(`js/history.js`)를 새로 만들어 적용했습니다. 외부 플러그인을 추가하지 않아 **가볍고**(아래 4번), 되돌리는 “단위”도 우리가 원하는 대로 조절했습니다.

**동작 방식**
- 입력이 **멈춘 지점**(약 0.4초 유휴)마다 본문 전체를 한 장의 스냅샷으로 기록합니다. → 연속해서 친 한 묶음이 **되돌리기 1단계**가 되어, **hwp처럼 한 번에 적당량씩** 되돌아갑니다(글자 하나하나로 잘게 쪼개지지 않음).
- 되돌리기/복원은 그 스냅샷으로 본문을 되돌린 뒤, **바뀐 부분 근처로 커서**를 옮겨 편집 흐름을 잇습니다.
- 스택 상한(60장)으로 메모리를 고정하고, 같은 내용은 중복 저장하지 않습니다.
- **페이지마다 독립**입니다(다른 페이지로 이동하면 그 페이지의 기록으로 새로 시작 — hwp가 문서별로 되돌리는 것과 동일).

**범위(주의)**
- 되돌리기/복원은 **본문 내용**(작성·삭제)을 대상으로 합니다. **제목 칸**과 **코드 블록 안**, 검색창 등 일반 입력칸은 **브라우저 기본 실행취소**(글자 단위)를 그대로 둬 세밀 편집을 보존합니다.
- 안전장치: 되돌리기로 본문의 하위문서 링크가 사라지더라도 **하위 페이지를 삭제하지 않습니다**(오삭제 방지). 실제로 페이지를 지우는 동기화는 사용자가 직접 편집할 때만 동작합니다.

## 2. 글자수 → 되돌리기/복원 버튼(← / →)

- 상단 우측 **저장 버튼 옆의 단어/글자 수 표기를 제거**하고, 그 자리에 **`←`(되돌리기)·`→`(복원)** 버튼을 넣었습니다. 화살표라 한눈에 들어옵니다.
- 버튼과 **단축키가 1:1로 연동**: `←` = `Ctrl/⌘+Z`, `→` = `Ctrl/⌘+Shift+Z`(또는 `Ctrl+Y`).
- 더 되돌릴/복원할 게 없으면 해당 버튼이 **흐리게 비활성**됩니다. **모바일 상단바**에도 같은 ← / → 버튼을 두었습니다.

## 3. 입력 중 자동 변환 (`js/autoformat.js`)

**인라인 기호** — 커서 앞 글자가 패턴이면 즉시 바뀝니다.

| 입력 | 변환 | 입력 | 변환 |
|---|---|---|---|
| `->` | → | `<-` | ← |
| `=>` | ⇒ | `<=` | ⇐ |
| `!=` | ≠ | `...` | … |
| `(c)` | © | `(r)` | ® |
| `(tm)` | ™ | | |

**줄머리 마크다운**(빈 줄 맨 앞에서, Notion 레퍼런스) — 접두어를 치면 블록이 바뀝니다.

- `[]` 또는 `[ ]` → **체크박스**(요청)
- `#` · `##` · `###` + 칸 → **제목 1·2·3**
- `-` · `*` + 칸 → **글머리 목록**, `1.` + 칸 → **번호 목록**
- `>` + 칸 → **인용구**,  ` ``` ` → **코드 블록**

**안전장치**
- **한글 조합 중(IME)** 에는 간섭하지 않습니다(조합이 깨지지 않음).
- **코드 블록·일반 입력칸**에서는 동작하지 않습니다(코드의 `->`, `=>` 등을 그대로 보존).
- 되돌리기/복원 적용 중에는 동작하지 않습니다.

## 4. 무거움 · 안정화 · 동기화 점검

- **추가 비용 측정:** 되돌리기 기록이 더하는 비용은 **변경 1건당 약 0.04ms**(스냅샷 복제+서명 2회 직렬화). 기존 본문 저장 `save()` 1회(≈3.6ms)에 비해 무시할 수준이라 입력/동기화 체감 저하 없음.
- **외부 의존 0 추가:** undo 플러그인 등을 붙이지 않고 순수 JS 모듈 2개(`history.js`, `autoformat.js`)만 추가. CDN 1개도 늘지 않음.
- **메모리 고정:** 스냅샷은 직렬화 가능한 순수 데이터, 상한 60장. 페이지 전환·웰컴 화면에서 기록을 비웁니다(누수 없음). 자동변환 리스너는 에디터 파괴 시 해제.
- **자동저장 멱등성 확인:** 되돌린 본문을 다시 그려도 `save()` 결과가 동일(서명 일치) → **복원(redo)이 끊기지 않음**을 검증.
- **검증(데모·브라우저 자동화):** 모듈 로드 정상, **콘솔 오류 0건**. 되돌리기/복원(내용·버튼 상태), 인라인 9종, 블록 변환(`[]`·`#`·`-`·`1.`·`>`·` ``` `), `Ctrl+Z` 단축키 가로채기 모두 정상.

---

## 바뀐 / 추가된 파일 (v6)
```
js/history.js     [신규] 되돌리기/복원 매니저(문서 스냅샷, hwp식 묶음 단위)
js/autoformat.js  [신규] 입력 자동변환(인라인 기호 + 줄머리 마크다운)
js/editor.js      onReady/teardown 에서 AutoFormat attach/detach
js/app.js         글자수 로직 제거 → History.reset/record, ←→ 버튼·Ctrl+Z 단축키,
                  되돌리기發 변경 시 하위문서 오삭제 방지(이벤트 detail.fromHistory)
index.html        글자수 span 제거 → ←/→ 버튼(데스크톱·모바일), 스크립트 2개 로드, 캐시버스트 v6
css/layout.css    .history-btn / .history-controls / .mobile-topbar-actions
css/components.css .word-count 규칙 제거
js/samples.js     ‘사용법’에 자동변환·되돌리기 안내 반영
js/local-store.js 데모 시드 버전 7 → 8 (사용법 갱신 반영)
```

## 검증 (데모 모드 · 브라우저 자동화)
- (1·2) 본문 편집 후 ← 또는 Ctrl+Z → 직전 묶음 되돌리기, → 또는 Ctrl+Shift+Z → 복원. 버튼 활성/비활성 정확.
- (3) `->`→→, `<-`→←, `=>`→⇒, `<=`→⇐, `!=`→≠, `...`→…, `(tm)`→™ / `[]`→체크리스트, `#`→제목, `-`→목록, `1.`→번호목록, `>`→인용구, ` ``` `→코드.
- (4) 기록 추가비용 0.04ms/건, save() 멱등(복원 보존), 콘솔 오류 0.
```
