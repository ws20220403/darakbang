# 다락방 v2 — 변경사항 요약

v1 배포 패키지를 “컨셉은 유지하고 내부를 전면 재설계”한 버전입니다.
디자인 언어(보라색 브랜드, Notion 스타일, 다크모드)는 그대로 두고, 깨져 있던 부분과 부족한 기능을 다시 만들었습니다.

---

## 요청하신 3가지

### 1. ‘추가’ 메뉴가 한 화면에 다 보이도록 (스크롤 제거)
- `+` 버튼·`/` 슬래시가 여는 블록 메뉴를 **그리드형(데스크톱 3열 / 모바일 2열)**으로 재설계.
- 14종 블록이 **스크롤 없이 한 화면에** 표시됩니다. 검색·키보드 이동·변환 기능은 그대로 유지.
- 구현: 네이티브 팝오버를 재스타일링(별도 메뉴를 새로 만들어 충돌시키지 않음 → 안정적).

### 2. 드래그 & 드롭으로 블록 이동
- v1에서 드래그를 무력화하던 “크롬 가드”(CSS `!important` 전쟁 + MutationObserver)를 **제거**.
- 블록 왼쪽 **손잡이(⠿)를 잡고 끌면** 순서를 바꿀 수 있고, **드롭 위치 표시선**이 나타납니다.
- 포인터 이벤트 기반이라 **마우스·터치(모바일) 모두** 동작. `위로/아래로` 버튼도 접근성용으로 유지.

### 3. 블록 보강
- **목록**(불릿·번호, 중첩) · **코드 블록** · **표** · **웹 북마크** · **목차(TOC, 헤더 자동수집)** 추가.
  - 기존: 문단·제목·체크리스트·인용구·구분선·토글·이미지·콜아웃·하위문서.

---

## 추가로 개선한 것 (전면 재설계 · 배포 수준)

| 구분 | 내용 |
|---|---|
| 데모 모드 | 로그인 없이 “데모로 둘러보기”로 즉시 체험(브라우저 localStorage 저장). 검증·소개에 유용 |
| 자동 저장 | 입력이 멈추면 ~1.5초 후 자동 저장 + 저장 시각 표시. 수동 저장(Ctrl+S)도 유지 |
| 안전 삭제 | 페이지 삭제 = **드라이브 휴지통 이동**(영구 삭제 X, 복구 가능) |
| 전체 검색 | 제목뿐 아니라 **본문 내용**까지 검색(저장 시 텍스트 인덱싱) |
| 이모지 검색 | “하트/별/책/동물…” 한글·영문 **키워드 검색**(기존엔 사실상 미작동) |
| 이름 바꾸기 | 브라우저 기본 `prompt()` → 앱 디자인에 맞는 **모달** |
| 저장소 추상화 | `storage.js`로 Drive/데모 백엔드 분리(같은 인터페이스) |
| 안정성 | 이미지 Blob URL **메모리 누수 수정**, 키보드 **포커스 링** 복원, 사이드바 정렬(rootPageOrder) 반영 |

---

## 검증 (로컬 데모 모드, 자동화 확인 완료)
- 데모 부팅 / 샘플 페이지 / 14종 블록 렌더(목록·코드·표·콜아웃·체크리스트 내용 포함)
- 추가 메뉴: 3×5 그리드, **스크롤 없음**, 뷰포트 내 표시
- 드래그: 블록 이동 + 드롭 표시선 동작
- 자동 저장 + 저장 시각, 전체검색(본문), 이모지 검색, 이름변경 모달
- 코드 블록 모노폰트/테마, 다크 모드

## 사용/배포
- 로컬 체험: 폴더에서 `python -m http.server 8080` → “데모로 둘러보기”
- 실제 사용: 로그인 화면 “⚙️ 구글 드라이브 연결 설정”에 Client ID 입력(코드 수정 불필요) 또는 `js/auth.js`의 `DEFAULT_CLIENT_ID` 교체. (자세한 내용은 `darakbang-v2-template/README.md`)

---

## 2차 보완 (UI/UX · 사용법 · 구글 연결)

- **텍스트 잘림 박멸**: 고정 높이를 최소 높이로, 긴 문자열은 줄바꿈, 한글 줄간격 보정, **추가 메뉴 항목 말줄임 제거**. 데스크톱~모바일(360px)에서 잘림 0건 확인. (`css/optimize.css`)
- **사용성/디자인**: 작은 글씨 대비 보정, 버튼/메뉴 최소 터치 영역, 모달 세로 넘침 시 스크롤.
- **사용법 샘플 문서**: 데모에 “📖 다락방 사용법” 페이지 기본 포함(추가/드래그/저장/검색/구글연결 안내).
- **구글 연결 턴키화**: 배포 후 **코드 수정 없이** 로그인 화면에서 Client ID 입력·저장. 현재 접속 주소를 그대로 안내(승인된 JavaScript 원본에 복사). 미설정 시 400 대신 설정 모달 안내.
- **버그 수정**: 열린 페이지가 없는데 변경상태(dirty)면 페이지 이동이 막히던 문제 수정.

---

## 3차 보완 (본문 폭 · 사용법 노트 · 로그인 기본/유지)

- **노트 본문 폭(중요 버그픽스)**: v1은 컨테이너 720 − 좌우패딩 96×2 = **실제 본문 528px**로 좁고 제목칸과 어긋났음. PC에서 **약 900px로 넓히고**, 화면이 좁아지면 가변, 모바일은 꽉 차게(검증: 데스크톱 893px / 모바일 343px, 잘림 0건).
- **상세 사용법 샘플 노트**: 콘텐츠를 `js/samples.js`로 공용화하여 **데모와 실제(구글) 모드 첫 실행 모두**에서 “다락방 사용법” 페이지를 **일반 노트로 생성**(수정·삭제 가능, 26블록 — 글쓰기/서식/추가/드래그/저장/페이지관리/검색/구글연결/로그인까지).
- **기본 화면 = 구글 로그인**: 실행하면 데모로 직행하지 않고 **로그인 화면**이 기본. 데모는 세션 한정(새로 실행하면 풀림).
- **로그인 상태 유지 옵션**: 로그인 화면 체크박스. 켜면 다음 실행에도 자동 로그인(localStorage), 끄면 이번 세션만(sessionStorage).

---

## 4차 보완 (실제 화면 확인하며 잡은 버그들)

- **코드 블록 내용 잘림 수정**: 코드 textarea가 내용에 맞춰 안 늘어나 마지막 줄이 잘리던 문제 → 로드 시·입력 시 자동 높이 조절(검증: 로드 99≈101, 12줄 입력 시 307로 늘어남).
- **+ / ⠿ 손잡이가 본문 텍스트와 겹치던 문제 수정**: `.ce-toolbar__content`를 `display:contents`로 정렬 기준을 본문에 맞추고, 손잡이를 본문 왼쪽 거터로 8px 띄움(검증: 겹침 0, gap 8px).
- **상단 경로(breadcrumb) 제목 글자 중간 잘림 수정**: 제목을 별도 span으로 감싸 깔끔한 말줄임 처리(검증: 전체 표시).
- **사용법 노트를 최상위·즐겨찾기로**: 하위에 접혀 안 보이던 것을 루트 첫 페이지 + 즐겨찾기로. 데모 시드 버전을 올려 기존 데모도 갱신, 실모드 기존 사용자도 1회 자동 생성.
- **본문 폭 추가 확대**: 본문 글 너비 상한 1040px(가변), 손잡이 거터 확보.
- **구분선 깨짐 수정**: 플러그인의 "***"가 회색 박스에 갇혀 보이던 것 → 깔끔한 1px 가로선으로(별표·회색배경 제거).
